# 嵌套属性路径解析实现分析

## 一、需求背景
当前绑定仅支持一级属性绑定（如 `Bind("Title", nameof(Text))`），无法支持嵌套对象属性绑定场景：
```csharp
// 希望支持的用法
view.Bind("User.Profile.AvatarUrl", nameof(SUIImage.Source));
view.Bind("Order.Status.Text", nameof(SUIText.Text));
```

## 二、核心实现思路
### 基本原理
将嵌套路径 `a.b.c` 拆分为独立属性访问链 `a → b → c`，逐个生成属性访问器并缓存，运行时链式调用获取最终值。

### 架构设计
```
属性路径 → 拆分 → 逐个生成访问器 → 组合成链式访问函数 → 缓存 → 运行时调用
```

## 三、具体实现方案
### 1. 路径解析逻辑
- 以 `.` 为分隔符拆分属性路径字符串
- 从根类型开始，逐级反射获取下一级属性的类型
- 逐个生成每一级属性的Getter访问器

### 2. 缓存机制
- 缓存Key格式：`类型全名:属性路径`（如 `MyApp.ViewModels.UserViewModel:Profile.AvatarUrl`）
- 首次解析后缓存组合后的链式访问函数，后续绑定直接复用
- 缓存存储在现有的 `ObjectAccessorCache` 中，复用已有缓存体系

### 3. 访问器组合
```csharp
// 伪代码示例
Func<object, object> BuildNestedGetter(List<Func<object, object>> getters)
{
    return obj =>
    {
        foreach (var get in getters)
        {
            if (obj == null) return null; // 中间空值短路
            obj = get(obj);
        }
        return obj;
    };
}
```

## 四、性能分析
### 开销对比
| 绑定类型 | 首次绑定开销 | 运行时开销 |
|---------|-------------|-----------|
| 一级属性 | 1次反射生成访问器 | 直接委托调用（≈1ns） |
| 3级嵌套属性 | 3次反射生成访问器 + 一次函数组合 | 3次委托调用（≈3ns） |

### 优化点
- 全路径缓存：同类型同路径的绑定只会解析一次
- 无运行时反射：所有访问逻辑都编译为委托调用，和手写代码性能接近
- 空值短路：中间节点为null时直接返回，避免后续无效调用

## 五、边界情况处理
### 1. 路径中间为空
示例：`User.Profile.AvatarUrl` 中 `Profile` 为null → 直接返回null，不抛异常
### 2. 属性不存在
绑定时校验路径有效性，提前抛出明确的异常信息，避免运行时报错
### 3. 动态类型支持
自动适配不同的实际类型，只要属性路径存在即可正常工作

## 六、兼容性说明
- **完全向后兼容**：原有一级属性绑定逻辑完全不变，性能无损失
- **无侵入式修改**：仅新增一个辅助方法，替换原有Getter获取入口即可
- **复用现有架构**：基于现有 `ObjectAccessorCache` 扩展，不需要新增缓存体系

## 七、扩展方向
### 双向绑定支持
嵌套属性的反向回写只需要按照同样思路生成链式Setter即可：
```csharp
void SetNestedValue(object root, object value, List<Func<object, object>> getters, Action<object, object> finalSetter)
{
    var current = root;
    for (int i = 0; i < getters.Count - 1; i++)
    {
        current = getters[i](current);
        if (current == null) return;
    }
    finalSetter(current, value);
}
```

### 索引器支持
后续可扩展支持 `List[0].Name`、`Dict["key"].Value` 等索引器路径格式
